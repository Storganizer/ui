import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.storganizer.app',
  appName: 'Storganizer',
  webDir: 'www',
  loggingBehavior: 'debug'
};

export default config;
